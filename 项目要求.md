我们希望做一个自动音乐转录（AMT）的项目，以钢琴为主，希望大致实现MT3、SeqToSeq的效果并做出某些创新。  2.我们希望做出 能看能用的前端，并实现 输入乐曲，输出五线谱 的一站式流程 3.我们作为学生项目，不是很追求一味的大规模，而是追求做到一些有意思的小创新 4.因为时间比较紧凑（一个月不到的时间），我们可能需要比较稳健的规划 

1.计算设备更换为一台八卡4090（如图），使用时间大致为一周（但现有的计算任务要保留，不要修改现有文件与任务） 2.我们决定做MiniMT3-Piano，创新点做约束解码 + 双模式输出 + pedal-aware cleanup，前端UI简洁实用为主，只做离线模式，最好直接输入乐曲输出五线谱 3.我们有两位同学，但其中一位同学没有技术基础，乐理基础较好，我有一定技术基础，但乐理基础薄弱 4.我们最终需要交付一份论文规范的小论文，包含Abstract，Introduction，Data&model，Experiments（包括实验，对比试验与消融实验（选做）），Results，FutureWork&Conclusion，References等，同时也需要有Figure与Table

环境为Conda环境，新建一个MiniMT3环境并配置

## 2026-05 收束阶段目标

- 不再继续超大规模训练或大幅改 backbone；v13/v15/v19 作为最终候选池。
- 主线改为：统一 full val 指标、统一 Test_new/Tst_new 展示输出、固定可复现的 decode/score preset。
- 最终推荐需要分成两套：`analysis_midi_best` 用于数值指标和演奏 MIDI，`score_demo_best` 用于 MusicXML/铺面展示。
- v19 xlarge 只在 full eval 或长音/offset 明显优于 v15 时进入主候选；否则作为 hybrid/teacher 辅助资源。
- 展示谱默认强制 `4/4`、`tempo=100`、必要时指定 key；保持 performance MIDI 与 MusicXML score pipeline 分离。

## 2026-05 收束结果

- 默认数值/演奏 MIDI 主模型：`v15_best`，`note_f1=0.5799 / offset_f1=0.2880 / pred_ref=0.956`，综合最稳。
- 最干净展示 base：`v13_best`，`note_f1=0.5344 / offset_f1=0.2538 / pred_ref=0.979`，漏音较多但 MusicXML 更稳。
- 最高 full-val 小 sweep 候选：`v19_selected`，`note_f1=0.5942 / offset_f1=0.3016 / pred_ref=0.753`，欠生成明显，不直接作为展示谱主模型。
- 当前优先展示候选：`v13+v19 hybrid`，Test_new 上 `chord_verticality=0.462`、visible rests `2`、voice collisions `13`，用于人工目检和展示谱打磨。
- 最终报告文件：`outputs/eval_final_mainline/final_metrics.json`、`outputs/eval_final_mainline/final_metrics.csv`、`outputs/demo_compare/final_demo_report.json`。
- 推理入口统一为 `scripts/final_infer.sh`：`MODE=analysis_midi` 用 v15 指标输出，`MODE=score_demo` 用 v15 展示谱，`MODE=hybrid_score` 用 v13+v19 当前展示候选。
- 后续只做小规模 calibration 与 score pipeline：优先调 `display_chord_long`、v19 per-pitch threshold、chord lock、voice assignment、long-note tie；不再开超大模型训练。

## 2026-05 v13+v19 hybrid 小收束

- 主线固定为 `v13_best + v19_selected`，不再训练新大模型；v15 只作为指标/fallback 对照。
- 新增双 preset：`hybrid_score` 负责展示谱，`hybrid_f1` 负责 note_f1/recall 诊断；展示谱也必须守住漏音、错音、和弦缺音。
- Hybrid 现在区分和弦补音、长音补音、重复长音延长，并输出对应 stats；`display_chord_long` 作为 `hybrid_score` alias 保留。
- 新增 pitch-aware calibration：从 eval JSON 的 `pitch_calibration.by_pitch` 生成 v19 assistant 的 per-pitch threshold bias，不改 checkpoint。
- 远端收束入口：`LAUNCH_HYBRID=1 bash scripts/remote_hybrid_converge.sh`；正式输出为 `outputs/eval_hybrid_converge/hybrid_metrics.json/csv` 和 `outputs/demo_compare/hybrid_demo_report.json`。
- 验收目标：`hybrid_f1_best` 尽量接近 v15 指标；`hybrid_score_best` 在 MusicXML 可打开、visible rests 低、voice collision 低的同时，不低于 v13 clean base 的 note/chord 指标。
