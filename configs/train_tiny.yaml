seed: 42
metadata: data/cache/maestro_tiny_index.json
val_metadata: data/cache/maestro_tiny_index.json
output_dir: outputs/ckpt_tiny
split: train
val_split: validation
max_items: 4
val_max_items: 2
train_seconds: 8.0
val_seconds: 8.0
train_sampling: fixed
val_sampling: fixed
batch_size: 1
eval_batch_size: 1
num_workers: 0
epochs: 50
lr: 0.0003
weight_decay: 0.01
betas: [0.9, 0.98]
warmup_steps: 20
grad_accum: 1
grad_clip: 1.0
precision: bf16
label_smoothing: 0.02
allow_tf32: true
compile: false
log_interval: 1
eval_interval: 20
save_interval: 100
model_config: configs/model.yaml
