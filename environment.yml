name: MiniMT3
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pytorch
  - torchaudio
  - pytorch-cuda=12.1
  - numpy
  - pyyaml
  - tqdm
  - pip:
      - -e .[eval,dev]
